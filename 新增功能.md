请在我现有的 AniCare AI / 安养智巡 项目基础上扩展功能，不要重构项目，不要改变整体视觉风格。

重要要求：
新增页面必须与原有页面保持一致：
- 继续使用 Next.js App Router + TypeScript + Tailwind CSS
- 继续使用现有 AppShell、Sidebar、TopNav 布局
- 继续使用现有 SectionHeader、StatCard、RiskBadge 等 UI 组件
- 延续当前暖色背景、Teal 主色、圆角卡片、轻阴影、Framer Motion 渐入动画
- 不要新增一套完全不同的设计语言
- 不要做蓝紫色 SaaS 风
- 所有新增功能看起来要像原系统自然扩展出来的模块

请新增 3 个核心功能页面：

一、风险调度中心 `/dispatch`

目标：
让系统不仅能识别风险，还能在多个事件同时发生时自动判断“先处理哪一个”。

页面内容：
1. 顶部统计卡片：
   - 当前待处理事件数
   - 紧急事件数
   - 平均响应时间
   - 调度完成率

2. 风险优先级队列：
   展示多个事件，按优先级自动排序。
   每个事件包含：
   - 事件类型
   - 风险等级
   - 发生区域
   - 等待时间
   - 受影响老人
   - AI 推荐处理顺序
   - 推荐理由

3. 调度建议卡片：
   显示 AI 推荐：
   “建议优先处理二楼走廊摔倒未响应事件，因为该事件风险等级为高，且已超过黄金响应时间 3 分钟。”

4. 操作按钮：
   - 指派护理员
   - 标记处理中
   - 查看应急流程
   - 查看事件详情

需要使用 mock 数据，并预留接口：
GET /api/dispatch
POST /api/dispatch/assign

二、老人行为画像页面 `/profiles`

目标：
体现系统不是只检测“有人异常”，而是能判断“这个老人今天是否和平时不一样”。

页面内容：
1. 老人列表卡片：
   - 姓名
   - 房间号
   - 年龄
   - 风险标签
   - 今日状态

2. 个人行为画像详情：
   点击老人后展示：
   - 平均起床时间
   - 平均活动时长
   - 常去区域
   - 夜间离床次数
   - 最近 7 天异常次数
   - 今日行为偏离度

3. 行为偏离分析：
   示例文案：
   “王阿姨通常 22:00 后不再离开房间，但今天 02:16 出现在走廊，且停留超过 6 分钟，系统判定为中风险异常活动。”

4. 图表：
   - 7 天活动趋势
   - 夜间离床次数趋势
   - 风险事件频次

预留接口：
GET /api/profiles
GET /api/profiles/:id

三、应急流程引导页面 `/emergency`

目标：
让 AI 决策从“给建议”升级为“指导护理员一步步处理”。

页面内容：
1. 事件选择区：
   支持选择不同事件：
   - 老人摔倒
   - 离床未归
   - 烟火异常
   - 长时间静止
   - 疑似突发疾病

2. 应急流程步骤卡片：
   每个事件有 4-6 个步骤。
   例如老人摔倒：
   - 第一步：确认老人意识状态
   - 第二步：观察是否出血或明显骨折
   - 第三步：不要盲目移动老人
   - 第四步：通知医护人员
   - 第五步：记录事件并生成护理报告

3. 每一步支持：
   - 点击“已完成”
   - 添加备注
   - 显示注意事项
   - 显示知识库依据

4. 右侧显示：
   - 当前风险等级
   - 已完成步骤数
   - 预计处理时间
   - 下一步建议

5. 完成后显示：
   - 生成事件处理记录
   - 导出处理报告按钮

预留接口：
GET /api/emergency/plans
POST /api/emergency/complete-step

同时请更新：
1. Sidebar 导航，加入：
   - 风险调度
   - 行为画像
   - 应急流程

2. mock-data.ts：
   增加 dispatchQueue、residentProfiles、emergencyPlans 三类 mock 数据。

3. types/index.ts：
   增加类型：
   - DispatchItem
   - ResidentProfile
   - BehaviorMetric
   - EmergencyPlan
   - EmergencyStep

4. API Routes：
   增加：
   - /api/dispatch
   - /api/profiles
   - /api/profiles/[id]
   - /api/emergency/plans

5. 不要删除或破坏现有页面：
   - dashboard
   - detect
   - decision
   - events
   - knowledge
   - about

6. 页面文案要像真实产品，不要像论文：
   不要写“提升管理效率”
   要写“系统先把最危险的事排到最前面”
   不要写“个性化行为建模”
   要写“不是所有老人凌晨出门都一样危险，要看他平时是不是这样”
   不要写“应急处置流程优化”
   要写“护理员不用临场想步骤，系统一步步带着做”

最终目标：
让网站看起来像一个已经可以投入康养机构试运行的真实 AI 安全管理系统，而不是单纯比赛展示页。
