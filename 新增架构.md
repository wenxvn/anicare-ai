
---

你是资深前端工程师，请在**现有项目代码**上进行增量改造，不要重构项目，不要破坏已有功能与路由。请严格遵守以下要求：

## 0. 项目背景与技术约束

* 项目主题：康养机构安全风险预警系统
* 当前技术栈：请读取现有项目（Next.js 14 + TypeScript + TailwindCSS + Framer Motion + Recharts，若有出入以仓库实际为准）
* 修改原则：**最小侵入式改造**，保持原有视觉风格、组件风格、交互习惯一致
* 文案视角：面向**值班主管**
* 禁止文案：不得出现“用于比赛展示/评委/竞赛加分”等措辞

---

## 1. 本次目标（功能层）

在现有系统上增加“深度学习能力可视化”的前端能力，做到：

1. 静态页面可运行
2. 使用高质量假数据模拟真实场景
3. 预留保守型 API 接口（仅占位，不强绑定后端）
4. 有图表与适度动效，体现多模态科技感

---

## 2. 页面策略（必须执行）

采用“**原页面增强 + 1个新增页面**”：

### A. 原页面增强（在已有监测/调度/决策页面中择优插入）

新增以下卡片或区块（复用现有布局体系）：

* 多模态融合风险卡（Fusion Risk Card）
* 15/30分钟短时风险预测卡
* 可解释预警卡（触发依据 + 处置建议 + 置信度）

### B. 新增页面

新增页面：`/prediction-center`（或与现有路由命名风格一致）
页面名建议：**多模态风险预测中心**
包含以下区域：

1. 全局风险趋势（时间序列图）
2. 未来15/30分钟风险热区/高风险房间列表
3. 多模态输入状态面板（视觉、床压、门磁、毫米波等可模拟）
4. 告警解释与处置建议队列
5. 模型运行状态（推理延迟、数据新鲜度、模型版本：均为前端假数据）

---

## 3. 三个核心模块（前端表现必须有）

### 模块1：多模态融合评分

* 展示输入源：

  * 视频行为识别分数
  * 床压状态
  * 门磁状态
  * 历史事件频率
* 输出：

  * 风险总分（0-100）
  * 风险等级（低/中/高）
  * 置信度（%）
* 需要可视化：仪表盘/进度环/堆叠条任选，但风格统一

### 模块2：短时风险预测（15/30分钟）

* 图表展示未来风险曲线（可用 Recharts）
* 显示“未来高风险TOP N房间/区域”
* 每条含：预测分、预计触发时间窗、建议巡查优先级

### 模块3：可解释预警

* 每条预警显示：

  * 触发原因（例如：夜间离床持续>12分钟、视觉异常姿态分数高、门磁频繁开合）
  * 贡献因子（百分比或权重）
  * 建议处置动作（面向值班主管）
* 支持状态流转按钮（待确认 / 已派单 / 已处理）仅前端态

---

## 4. 假数据与状态管理

* 建立统一 mock 数据文件（如 `src/mock/prediction.ts`）
* 假数据要“像真实业务”：

  * 不同风险等级分布
  * 时间序列有波动
  * 少量异常峰值
* 提供基础状态：

  * loading
  * empty
  * error（模拟接口失败）
* 不要写死在 JSX 内，抽离为数据层

---

## 5. API 预留（保守型）

只做接口占位与调用封装，不实现后端逻辑：

* 新建 API 文件（按现有项目风格）例如：

  * `getFusionRiskOverview()`
  * `getRiskForecast(params)`
  * `getAlertExplanations(params)`
* 若请求失败，自动回退到 mock 数据
* 统一返回结构遵循项目现有约定；若无约定，使用：

```ts
type ApiResp<T> = { code: number; message: string; data: T }
```

* 保持可替换性：后端接入时仅改 API 层即可

---

## 6. UI/动效要求

* 与现有站点配色、圆角、阴影、间距、字体一致
* 增加适度科技感：

  * 卡片轻微渐变/微发光边框（克制）
  * 数值变化过渡动画（Framer Motion）
  * 图表进入动画
* 禁止过度炫技，保证值班场景可读性优先
* 响应式适配桌面端（至少 1440 和 1920 宽度表现良好）

---

## 7. 文案规范（重要）

文案统一采用值班主管视角，示例风格：

* “未来30分钟2号楼东区风险上升，建议优先巡查”
* “当前高风险事件3条，建议先处理离床未归事件”
  避免：
* “本功能有利于比赛展示”
* “用于答辩”
* “评委可看到”

---

## 8. 素材要求

* 可使用免费可商用素材（如 Unsplash/Pexels）
* 优先关键词：elderly care, nursing station, smart monitoring, healthcare technology
* 若无合适图，使用统一风格占位图，不要风格混乱
* 注意图片压缩与加载性能

---

## 9. 代码产出要求

请直接输出可落地改动：

1. 新增/修改文件清单
2. 每个文件的完整代码（可直接覆盖）
3. 路由接入位置说明
4. mock 与 API 占位说明
5. 本地运行验证步骤

并确保：

* `npm run dev` 可启动
* `npm run lint` 无明显新增报错
* 不破坏原有页面功能

---

## 10. 验收标准（自检后再输出）

* [ ] 新增 `/prediction-center` 页面可访问
* [ ] 原页面已插入至少2个新增能力卡片
* [ ] 三个核心模块均有可视化呈现
* [ ] 有 loading/empty/error 三态
* [ ] API 占位 + mock fallback 生效
* [ ] 文案完全符合值班主管视角
* [ ] 风格与原系统一致且有适度科技感

---

如果现有仓库结构与你预期不一致，请先自动扫描目录并“就地适配”，不要另起一套新架构。
