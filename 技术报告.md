

## 📋 项目技术报告：安养智巡（AniCare AI）

---

### 一、项目概述

**安养智巡** 是一个面向养老院、康养中心、护理机构的 **AI 安全风险识别与智能决策平台**。系统通过视觉识别、行为理解和知识库决策，将康养机构中的高风险事件（如摔倒、离床未归、烟火异常等）提前识别并推送，帮助护理员快速判断事件优先级，实现从"事后翻监控"到"实时主动预警"的范式转变。

---

### 二、技术栈

| 层级 | 技术 | 版本 | 说明 |
|---|---|---|---|
| **框架** | Next.js (App Router) | 14.2.5 | 全栈 React 框架，使用 App Router 路由架构 |
| **语言** | TypeScript | 5.5.3 | 全项目使用 TS，严格模式开启 |
| **UI 库** | React / React DOM | 18.3.1 | 函数组件 + Hooks |
| **样式** | Tailwind CSS | 3.4.13 | 原子化 CSS 框架，自定义设计 token |
| **样式工具** | PostCSS + Autoprefixer | 8.4.47 / 10.4.20 | CSS 后处理与浏览器兼容 |
| **动画** | Framer Motion | 11.2.10 | 页面过渡、列表进入、渐显动画 |
| **图表** | Recharts | 2.12.7 | AreaChart、BarChart 数据可视化 |
| **图标** | @iconify/react | 5.0.2 | 使用 MDI 图标库，涵盖大量医疗/安全相关图标 |
| **工具** | clsx | 2.1.1 | 条件性 className 拼接 |
| **AI 后端** | OpenAI API (GPT-3.5-turbo) | — | 智能助手对话，通过 Next.js API Route 代理 |
| **部署** | Vercel | — | 配置了 vercel.json，支持一键部署 |

---

### 三、项目架构

```
src/
├── app/                        # Next.js App Router 页面目录
│   ├── page.tsx                # 首页（Landing）
│   ├── login/page.tsx          # 登录页
│   ├── dashboard/page.tsx      # 数据看板
│   ├── detect/page.tsx         # 智能检测
│   ├── decision/page.tsx       # AI 决策
│   ├── events/page.tsx         # 事件管理列表
│   ├── events/[id]/page.tsx    # 事件详情（动态路由）
│   ├── knowledge/page.tsx      # 智能助手（聊天）
│   ├── about/page.tsx          # 关于项目
│   ├── layout.tsx              # 根布局
│   ├── globals.css             # 全局样式 + Tailwind 配置
│   └── api/                    # API 路由
│       ├── chat/route.ts       # POST - AI 聊天（接 OpenAI）
│       ├── dashboard/route.ts  # GET - 看板数据
│       ├── decision/route.ts   # POST - AI 决策（Mock）
│       ├── events/route.ts     # GET - 事件列表
│       ├── events/[id]/route.ts# GET - 单事件详情
│       ├── knowledge/route.ts  # GET - 知识库
│       └── vision/route.ts     # POST - 视觉检测（Mock）
├── components/
│   ├── nav/
│   │   ├── app-shell.tsx       # 应用外壳（认证 + 布局）
│   │   ├── sidebar.tsx         # 侧边栏导航
│   │   └── top-nav.tsx         # 顶部导航栏
│   ├── decision/
│   │   └── live-decision.tsx   # 实时 AI 决策过程组件
│   └── ui/
│       ├── risk-badge.tsx      # 风险等级标签组件
│       ├── section-header.tsx  # 页面标题组件
│       └── stat-card.tsx       # 数据统计卡片组件
├── lib/
│   ├── api-client.ts           # 通用 fetch JSON 封装
│   ├── auth-context.tsx        # 认证上下文（React Context）
│   └── mock-data.ts            # Mock 数据（事件/看板/知识库/检测）
└── types/
    └── index.ts                # 全局类型定义
```

---

### 四、功能模块详解

#### 1. 用户认证系统

- **技术实现**：基于 React Context（`AuthContext`）+ `localStorage` 持久化
- **登录机制**：硬编码账号密码验证（默认 `123456/123456`），模拟 600ms 延迟
- **路由守卫**：`AppShell` 组件中通过 `useEffect` 检测登录状态，未登录自动跳转 `/login`
- **公开路径**：`/login` 不受认证保护，其余页面均需登录

#### 2. 智能检测模块（`/detect`）

- **图片上传**：支持用户本地上传图片或使用内置演示模式
- **检测流程**：分三个阶段模拟——"上传中" → "分析中"（逐步展示：调用视觉感知模型 → 识别人员姿态与行为 → 分析场景风险等级）→ "完成"
- **结果展示**：
  - 在图片上绘制 BoundingBox 标注检测区域（如"人员摔倒"框）
  - 展示检测结果列表（标签 + 置信度百分比）
  - 展示风险等级评分（87/100）及进度条
  - 提供跳转到"AI 决策"的入口
- **数据来源**：前端 Mock 数据（`mockDetectionResults`），后续可接入 YOLO/Pose Estimation 后端

#### 3. AI 决策模块（`/decision`）

- **决策过程可视化**：模拟 AI 分步决策——"读取知识库" → "分析风险等级" → "生成处置建议"，每步 800ms 延迟，逐步显示 ✅
- **知识库引用**：展示决策依据引用的知识库条目（如"跌倒应急处理流程"）
- **决策结果**：
  - 事件摘要 + 风险等级标签
  - 判断依据（带编号列表）
  - 处置建议（结构化文本）
- **实时决策组件**（`LiveDecision`）：嵌入在事件详情页中，以动画形式逐步展示决策过程

#### 4. 事件管理模块（`/events`）

- **事件列表**：展示 10 条模拟事件数据，包含事件类型、风险等级、处理状态、区域、摄像头、时间、处理人、置信度、摘要等
- **双维度筛选**：
  - 按处理状态筛选：全部 / 待处理 / 已通知 / 观察中 / 已处理
  - 按风险等级筛选：全部 / 紧急 / 高风险 / 中风险 / 低风险
- **状态样式**：不同处理状态使用不同颜色标签（红/橙/黄/绿）
- **事件详情**（`/events/[id]`）：
  - 动态路由，通过事件 ID 获取详情
  - 左侧：监控画面截图 + 识别摘要
  - 右侧：基本信息卡片 + 实时 AI 决策过程（`LiveDecision`）
  - 404 处理：事件不存在时展示空状态提示

#### 5. 数据看板模块（`/dashboard`）

- **核心指标卡片**（4 项）：今日事件总数、待处理高危事件、已处理率、平均响应时长
- **高风险事件趋势图**：使用 Recharts `AreaChart` 展示过去一周每日高风险事件数量
- **风险类型分布图**：使用 Recharts `BarChart` 展示各类风险事件数量（摔倒、离床未归、烟火异常等）
- **区域风险热度排名**：5 个区域的热度分数排行，使用进度条可视化
- **数据获取**：前端通过 `/api/dashboard` 获取，内含 fallback 默认数据

#### 6. 智能助手模块（`/knowledge`）

- **聊天界面**：完整的对话式 UI，支持用户消息和 AI 回复的气泡展示
- **快速提问**：预设 4 个常见问题（摔倒处理、离床处置、烟雾异常、压疮预防）
- **AI 后端**：通过 `/api/chat` 路由调用 OpenAI GPT-3.5-turbo API
  - 包含 System Prompt，注入知识库内容（5 条护理规范）
  - 设置 `temperature: 0.7`，`max_tokens: 1024`
- **交互体验**：
  - 消息进入动画（Framer Motion）
  - AI 回复等待时显示三点跳动动画
  - 自动滚动到最新消息
  - 发送按钮禁用状态

#### 7. 关于项目页面（`/about`）

- **项目背景与价值**：两张卡片分别介绍背景和价值
- **创新点对比**（4 项）：以"传统方案 vs 安养智巡"的对比卡片形式展示
  - 多模态场景感知
  - 主动风险检测
  - 知识增强决策
  - 秒级响应能力
- **适用场景**：展示 5 类适用场景（养老院、康养中心、社区日间照料、居家养老、医院康复病区）

---

### 五、UI/UX 设计特色

1. **色彩体系**：暖色调背景（`#f8f5f0` 奶白色）+ Teal 主色（`#0d9488`）+ 红/橙/黄/绿风险色阶
2. **圆角风格**：统一使用 `rounded-2xl`（1rem）和 `rounded-3xl`（1.25rem），营造柔和友好的视觉感
3. **卡片投影**：自定义 `card-glow` 工具类，hover 时增强阴影
4. **动画设计**：
   - 页面标题进入动画（SectionHeader）
   - 统计卡片、列表项渐入动画（Stagger 效果）
   - 检测/决策过程的步骤渐进展开
   - 聊天消息的入场动画
5. **响应式设计**：
   - 移动端侧边栏抽屉式展开（带遮罩层）
   - 网格布局自适应（sm/md/lg/xl 断点）
   - 顶部导航栏移动端显示汉堡菜单按钮
6. **字体**：Inter + Noto Sans SC，中英文混排优化

---

### 六、数据模型

| 接口 | 字段 |
|---|---|
| **DetectionResult** | label, confidence, bbox? |
| **DecisionPayload** | riskScore, cause, suggestion, basis?, knowledgeRefs? |
| **EventItem** | id, type, risk, zone, camera, time, status, handler, confidence, summary, decision, detections? |
| **DashboardStats** | todayEvents, retainedCritical, handledRate, avgResponseMinutes, riskTypeDistribution, weeklyCriticalTrend, zoneHotspots |
| **KnowledgeArticle** | id, title, tags, content, updatedAt, scenario |

---

### 七、API 接口一览

| 方法 | 路径 | 功能 | 状态 |
|---|---|---|---|
| POST | `/api/chat` | AI 智能助手对话 | ✅ 已对接 OpenAI |
| GET | `/api/dashboard` | 获取看板统计数据 | Mock |
| POST | `/api/decision` | AI 风险决策分析 | Mock |
| GET | `/api/events` | 获取事件列表 | Mock |
| GET | `/api/events/[id]` | 获取事件详情 | Mock |
| GET | `/api/knowledge` | 获取知识库内容 | Mock |
| POST | `/api/vision` | 视觉检测识别 | Mock |

---

### 八、总结

**安养智巡** 是一个完整的、可部署的前端演示系统，涵盖了从视觉感知、行为理解、智能决策到数据可视化的全链路设计。项目使用 Next.js App Router + TypeScript + Tailwind CSS 构建，具有清晰的模块划分和良好的代码组织。AI 聊天助手已实际对接 OpenAI API，其余视觉检测和决策模块以 Mock 数据模拟，预留了接入真实后端服务的接口。整体设计注重用户体验，动画流畅、交互友好，适合作为康养安全 AI 平台的前端原型或比赛演示。