# 安养智巡（AniCare AI）技术报告 v3.0

> 基于多模态感知与知识增强决策的康养机构安全风险预警系统

---

## 一、项目概述

### 1.1 项目定位

**安养智巡**是一个面向养老院、康养中心、护理机构的 **AI 安全风险识别与智能决策平台**。系统通过视觉识别、行为理解和知识库决策，将康养机构中的高风险事件（如摔倒、离床未归、烟火异常等）提前识别并推送给护理员，帮助其快速判断事件优先级，实现从"事后翻监控"到"实时主动预警"的范式转变。

### 1.2 核心价值

传统监控系统只负责录像，老人摔倒、离床、烟火等风险往往要等下一轮巡房甚至事故发生后才被发现。安养智巡的核心价值在于：

- **主动风险检测**：系统持续检测风险，一有异常立即通知，不再依赖人工巡查间隔。
- **知识增强决策**：不只是报警，还会给出风险等级、判断依据和具体处置建议。
- **智能优先级排序**：多个事件同时发生时，系统先把最危险的事排到最前面，护理员一看就知道先处理哪件事。
- **秒级响应能力**：从检测到通知平均不超过 4 分钟，远优于传统的 30 分钟以上发现周期。

### 1.3 参赛信息

- **赛事**：中国机器人及人工智能大赛 —— 人工智能创新赛
- **系统全称**：基于多模态感知与知识增强决策的康养机构安全风险预警系统
- **产品名**：安养智巡（AniCare AI）

---

## 二、技术栈

### 2.1 前端技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **框架** | Next.js (App Router) | 14.2.5 | 全栈 React 框架，使用 App Router 路由架构 |
| **语言** | TypeScript | 5.5.3 | 全项目使用 TS，严格模式开启 |
| **UI 库** | React / React DOM | 18.3.1 | 函数组件 + Hooks |
| **样式** | Tailwind CSS | 3.4.13 | 原子化 CSS 框架，自定义设计 token |
| **样式工具** | PostCSS + Autoprefixer | 8.4.47 / 10.4.20 | CSS 后处理与浏览器兼容 |
| **动画** | Framer Motion | 11.2.10 | 页面过渡、列表进入、渐显动画 |
| **图表** | Recharts | 2.12.7 | AreaChart、BarChart 数据可视化 |
| **图标** | @iconify/react | 5.0.2 | 使用 MDI 图标库，涵盖大量医疗/安全相关图标 |
| **工具** | clsx | 2.1.1 | 条件性 className 拼接 |

### 2.2 后端 / AI 技术栈（规划）

| 层级 | 技术 | 说明 |
|------|------|------|
| **视觉感知** | YOLO / Pose Estimation / Video Understanding | 目标检测、姿态估计与视频理解 |
| **行为理解** | 时序分析 / 场景关系判断 / 风险规则引擎 | 从"看到"升级为"看懂" |
| **智能决策** | LLM Agent / RAG 知识库 / 风险评分 | 不只报警，还能给出判断依据和处置建议 |
| **AI 对话** | OpenAI API (GPT-3.5-turbo) | 智能助手对话，已通过 Next.js API Route 实际对接 |
| **后端框架** | FastAPI + Uvicorn (规划) | Python 后端，当前阶段使用 Mock 数据 |

### 2.3 部署与环境

| 项目 | 说明 |
|------|------|
| **部署平台** | Vercel（已配置 vercel.json） |
| **运行环境** | Node.js 18+，Python 3.11.9（后端规划） |
| **包管理** | npm（前端），pip / conda（后端） |
| **环境变量** | .env.example 模板，支持 OPENAI_API_KEY、YOLO_ENDPOINT 等配置 |

---

## 三、项目架构

### 3.1 目录结构

```
CARIC/
├── anicare-ai/                      # Next.js 前端项目
│   ├── src/
│   │   ├── app/                     # Next.js App Router 页面目录
│   │   │   ├── page.tsx             # 首页（Landing Page）
│   │   │   ├── layout.tsx           # 根布局（AppShell 包裹）
│   │   │   ├── globals.css          # 全局样式 + Tailwind CSS 变量
│   │   │   ├── login/page.tsx       # 登录页
│   │   │   ├── detect/page.tsx      # 智能检测页面
│   │   │   ├── decision/page.tsx    # AI 决策页面
│   │   │   ├── dispatch/page.tsx    # 风险调度中心
│   │   │   ├── profiles/page.tsx    # 老人行为画像
│   │   │   ├── emergency/page.tsx   # 应急流程引导
│   │   │   ├── events/page.tsx      # 事件管理列表
│   │   │   ├── events/[id]/page.tsx # 事件详情（动态路由）
│   │   │   ├── dashboard/page.tsx   # 数据看板
│   │   │   ├── knowledge/page.tsx   # 智能助手（聊天）
│   │   │   ├── about/page.tsx       # 关于项目
│   │   │   └── api/                 # Next.js API Routes
│   │   │       ├── vision/route.ts          # POST - 视觉检测
│   │   │       ├── behavior/analyze/route.ts # POST - 行为分析
│   │   │       ├── decision/route.ts        # POST - AI 决策
│   │   │       ├── dispatch/route.ts        # GET/POST - 调度队列
│   │   │       ├── emergency/plans/route.ts # GET/POST - 应急方案
│   │   │       ├── events/route.ts          # GET - 事件列表
│   │   │       ├── events/[id]/route.ts     # GET - 事件详情
│   │   │       ├── profiles/route.ts        # GET - 住户列表
│   │   │       ├── profiles/[id]/route.ts   # GET - 住户详情
│   │   │       ├── dashboard/route.ts       # GET - 看板数据
│   │   │       ├── knowledge/route.ts       # GET - 知识库
│   │   │       └── chat/route.ts            # POST - AI 聊天（对接 OpenAI）
│   │   ├── components/
│   │   │   ├── nav/
│   │   │   │   ├── app-shell.tsx    # 应用外壳（认证 + 布局管理）
│   │   │   │   ├── sidebar.tsx      # 侧边栏导航（10 个菜单项）
│   │   │   │   └── top-nav.tsx      # 顶部导航栏
│   │   │   ├── ui/
│   │   │   │   ├── risk-badge.tsx   # 风险等级标签组件
│   │   │   │   ├── section-header.tsx # 页面标题组件
│   │   │   │   └── stat-card.tsx    # 数据统计卡片组件
│   │   │   └── decision/
│   │   │       └── live-decision.tsx # 实时 AI 决策过程组件
│   │   ├── services/                # 业务服务层
│   │   │   ├── vision.service.ts    # 视觉检测服务（Mock + YOLO 适配器）
│   │   │   ├── behavior.service.ts  # 行为分析服务（偏离度计算引擎）
│   │   │   ├── decision.service.ts  # 决策服务（规则引擎 + LLM 适配器）
│   │   │   ├── dispatch.service.ts  # 调度服务（优先级计算 + 队列管理）
│   │   │   └── emergency.service.ts # 应急服务（步骤执行 + 报告生成）
│   │   ├── lib/
│   │   │   ├── api-client.ts        # 通用 fetch JSON 封装
│   │   │   ├── api-response.ts      # 统一 API 响应格式封装
│   │   │   ├── auth-context.tsx     # 认证上下文（React Context）
│   │   │   └── mock-data.ts         # Mock 数据（事件/看板/知识库/检测/画像/应急）
│   │   └── types/
│   │       └── index.ts             # 全局类型定义（30+ 接口/类型）
│   ├── package.json                 # 项目依赖
│   ├── tsconfig.json                # TypeScript 配置
│   ├── tailwind.config.ts           # Tailwind CSS 配置（自定义设计 token）
│   ├── postcss.config.mjs           # PostCSS 配置
│   ├── next.config.mjs              # Next.js 配置（远程图片白名单）
│   ├── vercel.json                  # Vercel 部署配置
│   └── .gitignore                   # Git 忽略规则
├── CRAIC/                           # Python 虚拟环境（conda）
├── requirements.txt                 # Python 依赖（FastAPI 栈）
├── environment.yml                  # Conda 环境配置
├── .env.example                     # 环境变量模板
├── gen_env.py                       # 环境配置生成脚本
├── start.bat                        # 一键启动脚本
└── 技术报告.md                       # 技术报告 v1
```

### 3.2 分层架构设计

系统采用清晰的分层架构，前端与后端通过 API 层解耦，各服务层使用适配器模式支持 Mock / Real 双模式切换：

```
┌──────────────────────────────────────────────────────────────┐
│                    前端展示层（Pages）                         │
│  Landing · Detect · Decision · Dispatch · Profiles ·         │
│  Emergency · Events · Dashboard · Knowledge · About          │
├──────────────────────────────────────────────────────────────┤
│                    UI 组件层（Components）                     │
│  AppShell · Sidebar · TopNav · RiskBadge · StatCard ·        │
│  SectionHeader · LiveDecision                                │
├──────────────────────────────────────────────────────────────┤
│                    API 路由层（Next.js API Routes）            │
│  /api/vision · /api/behavior · /api/decision ·               │
│  /api/dispatch · /api/emergency · /api/events ·              │
│  /api/profiles · /api/dashboard · /api/knowledge · /api/chat │
├──────────────────────────────────────────────────────────────┤
│                    业务服务层（Services）                       │
│  VisionService · BehaviorService · DecisionService ·         │
│  DispatchService · EmergencyService                          │
├──────────────────────────────────────────────────────────────┤
│                    数据层（Mock Data + Types）                 │
│  mock-data.ts · types/index.ts · api-client.ts               │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、核心功能模块

### 4.1 用户认证系统

**技术实现**：基于 React Context（`AuthContext`）+ `localStorage` 持久化

- **登录机制**：硬编码账号密码验证（默认 `123456/123456`），模拟 600ms 延迟以增强真实感
- **路由守卫**：`AppShell` 组件中通过 `useEffect` 检测登录状态，未登录自动跳转 `/login`
- **公开路径**：仅 `/login` 不受认证保护，其余所有页面均需登录后访问
- **退出登录**：清除 `localStorage` 并跳转至登录页

**关键代码路径**：
- 认证上下文：[auth-context.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/lib/auth-context.tsx)
- 应用外壳：[app-shell.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/components/nav/app-shell.tsx)

### 4.2 智能检测模块（`/detect`）

**功能描述**：用户上传监控画面图片，系统自动识别画面中的风险场景并给出风险等级判断。

**技术实现**：
- **图片上传**：支持用户本地文件上传或使用内置演示模式（一键体验）
- **检测流程模拟**：分三个阶段——"上传中" → "分析中"（逐步展示：调用视觉感知模型 → 识别人员姿态与行为 → 分析场景风险等级）→ "完成"
- **结果展示**：
  - 在图片上动态绘制 BoundingBox 标注检测区域
  - 展示检测结果列表（标签 + 置信度百分比）
  - 展示风险等级评分（87/100）及进度条
  - 提供跳转到"AI 决策建议"的入口
- **服务层设计**：`VisionService` 使用适配器模式，当前为 `MockVisionAdapter`，预留 `YoloVisionAdapter` 接口，可通过环境变量 `AI_MODE=real` 切换

**关键代码路径**：
- 页面组件：[detect/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/detect/page.tsx)
- 视觉服务：[vision.service.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/services/vision.service.ts)

### 4.3 AI 决策模块（`/decision`）

**功能描述**：系统结合检测结果和知识库，为护理员生成结构化的风险判断依据和处置建议。

**技术实现**：
- **决策过程可视化**：模拟 AI 分步决策——"读取知识库" → "分析风险等级" → "生成处置建议"，每步 800ms 延迟，逐步显示完成状态
- **知识库引用**：展示决策依据引用的知识库条目（如"跌倒应急处理流程"）
- **决策结果**：事件摘要 + 风险等级标签 + 判断依据（带编号列表）+ 处置建议
- **实时决策组件**（`LiveDecision`）：嵌入在事件详情页中，以动画形式逐步展示完整的 4 步决策过程
- **决策引擎**：`DecisionService` 内置 `RuleBasedDecisionEngine`（规则引擎），支持通过 `LLMDecisionAdapter` 接入大模型，规则引擎降级兜底

**关键代码路径**：
- 页面组件：[decision/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/decision/page.tsx)
- 决策服务：[decision.service.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/services/decision.service.ts)
- 实时决策组件：[live-decision.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/components/decision/live-decision.tsx)

### 4.4 行为分析模块（服务层）

**功能描述**：基于老人日常行为习惯数据，计算行为偏离度评分和风险趋势判断。

**技术实现**：
- **偏离度计算**：综合多个因子——时间段（夜间加分）、夜间离床次数、周异常次数、检测到跌倒/静止等异常行为
- **风险趋势判定**：偏离度 > 70 或周异常 > 4 次判定为"上升"，< 30 判定为"下降"，其余为"稳定"
- **异常行为标签识别**：自动从检测结果中提取跌倒行为、长时间静止、无人响应等标签
- **住户画像关联**：支持通过 `residentId` 直接获取住户的偏离度数据

**关键代码路径**：
- 行为服务：[behavior.service.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/services/behavior.service.ts)

### 4.5 风险调度中心（`/dispatch`）

**功能描述**：多个事件同时发生时，系统根据风险等级、等待时间和影响范围自动排序，把最危险的事件排到最前面。

**技术实现**：
- **优先级评分算法**：综合风险等级权重（critical=100, high=70, medium=40, low=15）+ 等待时间因子（每分钟 1.5 分，上限 40）+ 影响人数因子（多人 +15）+ 置信度因子（>0.9 则 +5），总分上限 100
- **队列排序**：已完成事件降级，处理中事件低于待指派，同状态按优先级分数降序
- **统计指标**：实时计算待处理数、紧急事件数、平均等待时长、调度完成率
- **AI 调度建议**：自动识别最高优先级事件并生成自然语言调度建议
- **操作支持**：指派护理员、标记处理中、查看应急流程、查看事件详情

**关键代码路径**：
- 页面组件：[dispatch/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/dispatch/page.tsx)
- 调度服务：[dispatch.service.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/services/dispatch.service.ts)

### 4.6 老人行为画像（`/profiles`）

**功能描述**：为每位老人建立日常行为基线，一旦今天跟平时不一样，马上提醒护理员。

**技术实现**：
- **住户列表**：卡片式展示所有住户的基本信息（姓名、房间、年龄）+ 风险标签 + 当日状态（带状态灯：红/橙/黄/绿）
- **行为画像详情**：
  - 日常习惯数据：平均起床时间、日均活动时长、常去区域、周均夜间离床次数、近 7 天异常次数、今日偏离度
  - 7 天活动趋势图（AreaChart）
  - 夜间离床次数趋势（BarChart）
  - 风险事件频次趋势（BarChart）
  - 行为偏离分析（进度条 + 文字摘要）
  - 当日状态 + 常去区域 + 风险标签
- **数据可视化**：使用 Recharts 的 AreaChart 和 BarChart 组件，Tooltip 自定义样式

**关键代码路径**：
- 页面组件：[profiles/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/profiles/page.tsx)
- 类型定义：[types/index.ts - ResidentProfile](file:///d:/wenxvn/CARIC/anicare-ai/src/types/index.ts)

### 4.7 应急流程引导（`/emergency`）

**功能描述**：护理员不用临场想步骤，系统一步步带着做。选一个事件类型，按步骤处理，每一步都有知识库撑腰。

**技术实现**：
- **5 套应急方案**：
  1. 老人摔倒（5 步，预计 8 分钟）
  2. 离床未归（5 步，预计 6 分钟）
  3. 烟火异常（5 步，预计 10 分钟）
  4. 长时间静止（5 步，预计 5 分钟）
  5. 疑似突发疾病（6 步，预计 12 分钟）
- **步骤状态机**：每步支持 `pending` → `doing` → `done` / `skipped` 状态转换
- **进度跟踪**：实时计算完成进度百分比，展示当前执行步骤和下一步建议
- **知识库引用**：每一步都关联具体的知识库条目依据
- **备注功能**：支持为每个步骤添加处理备注
- **处理记录生成**：所有步骤完成后可生成结构化的事件处理记录报告

**关键代码路径**：
- 页面组件：[emergency/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/emergency/page.tsx)
- 应急服务：[emergency.service.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/services/emergency.service.ts)

### 4.8 事件管理模块（`/events`）

**功能描述**：系统自动识别并归类所有风险事件，支持按处理状态和风险等级双维度筛选。

**技术实现**：
- **事件列表**：展示 10 条模拟事件数据，包含事件类型、风险等级、处理状态、区域、摄像头、时间、处理人、置信度、摘要
- **双维度筛选**：
  - 按处理状态：全部 / 待处理 / 已通知 / 观察中 / 已处理
  - 按风险等级：全部 / 紧急 / 高风险 / 中风险 / 低风险
- **状态样式**：不同处理状态使用不同颜色标签（红/橙/黄/绿）
- **事件详情**（`/events/[id]`）：
  - 动态路由，通过事件 ID 获取详情
  - 左侧：监控画面截图 + 识别摘要
  - 右侧：基本信息卡片 + 实时 AI 决策过程（`LiveDecision` 组件）
  - 404 处理：事件不存在时展示空状态提示

**关键代码路径**：
- 列表页面：[events/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/events/page.tsx)
- 详情页面：[events/\[id\]/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/events/%5Bid%5D/page.tsx)

### 4.9 数据看板模块（`/dashboard`）

**功能描述**：实时监控今日关键指标，帮护理员看清全局。

**技术实现**：
- **核心指标卡片**（4 项）：
  - 今日事件总数（自动检测并归类的风险事件）
  - 待处理高危事件（已标注紧急且尚未处理的事件）
  - 已处理率（今天所有事件中已处理的比例）
  - 平均响应时长（系统从识别到通知护理员的平均时间）
- **高风险事件趋势图**：AreaChart 展示过去一周每日高风险事件数量
- **风险类型分布图**：BarChart 展示各类风险事件数量（摔倒、离床未归、烟火异常、久卧未动、异常滞留等 8 类）
- **区域风险热度排名**：5 个区域的热度分数排行，使用进度条可视化
- **数据获取**：前端通过 `/api/dashboard` 获取，内含 fallback 默认数据确保网络异常时正常展示

**关键代码路径**：
- 页面组件：[dashboard/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/dashboard/page.tsx)

### 4.10 智能助手模块（`/knowledge`）

**功能描述**：护理员可以随时向 AI 助手提问护理安全方面的问题，系统结合知识库给出专业处置建议。

**技术实现**：
- **聊天界面**：完整的对话式 UI，支持用户消息和 AI 回复的气泡展示
- **快速提问**：预设 4 个常见问题（摔倒处理、离床处置、烟雾异常、压疮预防）
- **AI 后端**：通过 `/api/chat` 路由实际对接 OpenAI GPT-3.5-turbo API
  - System Prompt 注入 5 条护理规范知识库内容
  - temperature: 0.7，max_tokens: 1024
- **交互体验**：
  - 消息进入动画（Framer Motion）
  - AI 回复等待时显示三点跳动动画
  - 自动滚动到最新消息
  - 发送按钮禁用状态

**关键代码路径**：
- 页面组件：[knowledge/page.tsx](file:///d:/wenxvn/CARIC/anicare-ai/src/app/knowledge/page.tsx)
- API 路由：[chat/route.ts](file:///d:/wenxvn/CARIC/anicare-ai/src/app/api/chat/route.ts)

---

## 五、服务层设计模式

### 5.1 适配器模式（Adapter Pattern）

系统的核心服务层（Vision、Decision）采用适配器模式，支持 Mock / Real 双模式切换：

```
VisionService
├── VisionAdapter (接口)
│   ├── MockVisionAdapter   ← 当前使用，返回模拟数据
│   └── YoloVisionAdapter   ← 预留，对接真实 YOLO 服务
└── getAdapter()             ← 根据 AI_MODE 环境变量选择

DecisionService
├── LLMAdapter (接口)        ← 预留，对接真实 LLM 服务
├── RAGAdapter (接口)        ← 预留，对接 RAG 知识库检索
├── RuleBasedDecisionEngine  ← 当前使用，基于规则引擎
└── evaluate()               ← Real 模式下先尝试 LLM，失败降级到规则引擎
```

### 5.2 统一 API 响应格式

所有 API 路由使用统一的响应格式封装：

```typescript
interface ApiResponse<T> {
  success: boolean;     // 请求是否成功
  data?: T;            // 响应数据
  error?: string;      // 错误信息
  message?: string;    // 附加消息
  timestamp: string;   // 响应时间戳
}
```

通过 `apiSuccess()` 和 `apiError()` 工具函数确保所有接口响应格式一致。

### 5.3 优先级评分算法

调度服务使用多因子加权评分算法：

```
priorityScore = riskWeight + waitFactor + peopleFactor + confidenceFactor

其中：
- riskWeight: critical=100, high=70, medium=40, low=15
- waitFactor: min(等待分钟数 × 1.5, 40)
- peopleFactor: 多人受影响时 +15
- confidenceFactor: 置信度 > 0.9 时 +5
- 上限: min(100, 总分)
```

---

## 六、数据模型

### 6.1 核心类型定义

| 类型名 | 说明 | 关键字段 |
|--------|------|----------|
| `RiskLevel` | 风险等级枚举 | `'low' \| 'medium' \| 'high' \| 'critical'` |
| `DetectionResult` | 检测结果 | label, confidence, bbox?, category? |
| `VisionDetectInput` | 视觉检测输入 | imageUrl?, imageBase64?, videoUrl?, cameraId?, zone? |
| `VisionDetectOutput` | 视觉检测输出 | detections[], processingTimeMs, modelVersion |
| `DecisionInput` | 决策输入 | detections[], eventContext?, behaviorAnalysis?, knowledgeRefs? |
| `DecisionOutput` | 决策输出 | riskScore, riskLevel, priority, suggestions[], reasoning[], cause |
| `BehaviorAnalysis` | 行为分析 | deviationScore, behaviorLabels[], riskTrend, summary, details[] |
| `EventItem` | 事件条目 | id, type, risk, riskLevel, zone, camera, time, status, handler, confidence, summary, decision |
| `DecisionPayload` | 决策载荷 | riskScore, riskLevel, cause, suggestion, basis?, knowledgeRefs? |
| `DispatchItem` | 调度条目 | id, eventId, type, risk, riskLevel, zone, waitMinutes, residentName, priority, priorityScore, reason, status |
| `ResidentProfile` | 住户画像 | id, name, room, age, riskTags[], todayStatus, avgWakeTime, avgActiveHours, frequentZones[], nightLeaveCount, weeklyAnomalies, todayDeviation, deviationSummary |
| `EmergencyPlan` | 应急方案 | id, eventType, icon, riskLevel, estimatedMinutes, status, steps[] |
| `EmergencyStep` | 应急步骤 | id, order, title, note, knowledgeRef, status, completedAt?, remark? |
| `DashboardStats` | 看板统计 | todayEvents, retainedCritical, handledRate, avgResponseMinutes, riskTypeDistribution[], weeklyCriticalTrend[], zoneHotspots[] |
| `KnowledgeArticle` | 知识库条目 | id, title, tags[], content, updatedAt, scenario |
| `ApiResponse<T>` | API 响应 | success, data?, error?, message?, timestamp |

### 6.2 风险等级评分映射

| 等级 | 英文标识 | 分数区间 | 颜色 |
|------|----------|----------|------|
| 低风险 | `low` | 0-39 | 绿色 (emerald) |
| 中风险 | `medium` | 40-64 | 琥珀色 (amber) |
| 高风险 | `high` | 65-84 | 橙色 (orange) |
| 紧急 | `critical` | 85-100 | 红色 (red) |

---

## 七、API 接口一览

### 7.1 已实现的 API 路由

| 方法 | 路径 | 功能 | 数据来源 |
|------|------|------|----------|
| POST | `/api/vision/detect` | 视觉检测识别 | VisionService（Mock/Real） |
| POST | `/api/behavior/analyze` | 行为偏离度分析 | BehaviorService（Mock 引擎） |
| POST | `/api/decision/evaluate` | AI 风险决策评估 | DecisionService（规则引擎/LLM） |
| GET | `/api/dispatch` | 获取调度优先级队列 | DispatchService |
| POST | `/api/dispatch` | 指派护理员处理事件 | DispatchService |
| GET | `/api/emergency/plans` | 获取应急方案列表 | EmergencyService |
| POST | `/api/emergency/plans` | 执行应急步骤 | EmergencyService |
| GET | `/api/events` | 获取事件列表 | Mock 数据 |
| GET | `/api/events/[id]` | 获取事件详情 | Mock 数据 |
| GET | `/api/profiles` | 获取住户画像列表 | Mock 数据 |
| GET | `/api/profiles/[id]` | 获取单个住户画像 | Mock 数据 |
| GET | `/api/dashboard` | 获取看板统计数据 | Mock 数据 |
| GET | `/api/knowledge` | 获取知识库内容 | Mock 数据 |
| POST | `/api/chat` | AI 智能助手对话 | **已对接 OpenAI GPT-3.5-turbo** |

### 7.2 前端数据获取策略

所有页面组件采用统一的数据获取策略：
1. 首先尝试通过 `fetchJson()` 从 API 获取数据
2. 若请求失败，使用页面内预定义的 `fallback` 默认数据
3. 确保网络异常时页面仍能正常展示

---

## 八、UI/UX 设计

### 8.1 设计风格

**设计定位**：反主流 SaaS 模板，追求"科技感但有温度"的康养美学。

- **色彩体系**：暖色调背景（`#f8f5f0` 奶白色）+ Teal 主色（`#0d9488`）+ 红/橙/黄/绿风险色阶
- **圆角风格**：统一使用 `rounded-2xl`（1rem）和 `rounded-3xl`（1.25rem），营造柔和友好的视觉感
- **卡片投影**：自定义 `card-glow` 工具类，默认轻微阴影，hover 时增强
- **字体**：Inter + Noto Sans SC，中英文混排优化

### 8.2 动画设计

- **页面标题进入**：SectionHeader 组件使用 Framer Motion 的 fadeIn + slideUp 动画
- **列表项渐入**：统计卡片、列表项使用 Stagger 效果（延迟递增 0.06s）
- **检测/决策过程**：步骤渐进展开，模拟真实 AI 处理过程
- **聊天消息**：用户/AI 消息气泡带入场动画
- **loading 状态**：三点跳动动画（CSS animation + stagger delay）

### 8.3 响应式设计

- **移动端**：侧边栏抽屉式展开（带遮罩层），顶部导航栏显示汉堡菜单按钮
- **网格布局**：自适应 sm/md/lg/xl 四个断点
- **桌面端**：侧边栏固定展示，主内容区自适应宽度

### 8.4 风险等级视觉体系

| 等级 | 背景色 | 文字色 | 边框色 | 状态灯 |
|------|--------|--------|--------|--------|
| 紧急 | `bg-red-50` | `text-red-600` | `border-red-200` | 红色闪烁 |
| 高风险 | `bg-orange-50` | `text-orange-600` | `border-orange-200` | 橙色 |
| 中风险 | `bg-amber-50` | `text-amber-600` | `border-amber-200` | 琥珀色 |
| 低风险 | `bg-emerald-50` | `text-emerald-600` | `border-emerald-200` | 绿色 |

---

## 九、页面路由结构

| 路由 | 页面名称 | 认证要求 | 说明 |
|------|----------|----------|------|
| `/` | 首页 | 需登录 | Landing Page，核心能力展示 + 快速入口 |
| `/login` | 登录 | 公开 | 登录表单，支持账号/密码 |
| `/detect` | 智能检测 | 需登录 | 图片上传 + 风险检测演示 |
| `/decision` | AI 决策 | 需登录 | 决策过程可视化 + 结果展示 |
| `/dispatch` | 风险调度 | 需登录 | 优先级队列 + 指派操作 |
| `/profiles` | 行为画像 | 需登录 | 住户列表 + 详细行为分析 |
| `/emergency` | 应急流程 | 需登录 | 应急方案选择 + 步骤引导 |
| `/events` | 事件管理 | 需登录 | 事件列表 + 双维度筛选 |
| `/events/[id]` | 事件详情 | 需登录 | 动态路由，事件详情 + AI 决策 |
| `/dashboard` | 数据看板 | 需登录 | 核心指标 + 趋势图表 |
| `/knowledge` | 智能助手 | 需登录 | AI 聊天对话 |
| `/about` | 关于项目 | 需登录 | 项目背景 + 创新点 + 适用场景 |

---

## 十、导航结构

侧边栏包含 10 个导航项，覆盖系统的全部核心功能：

| 序号 | 图标 | 标签 | 路由 |
|------|------|------|------|
| 1 | `mdi:home-heart` | 首页 | `/` |
| 2 | `mdi:eye-check-outline` | 智能检测 | `/detect` |
| 3 | `mdi:brain` | AI 决策 | `/decision` |
| 4 | `mdi:broadcast` | 风险调度 | `/dispatch` |
| 5 | `mdi:account-details` | 行为画像 | `/profiles` |
| 6 | `mdi:ambulance` | 应急流程 | `/emergency` |
| 7 | `mdi:alert-octagon-outline` | 事件管理 | `/events` |
| 8 | `mdi:chart-areaspline` | 数据看板 | `/dashboard` |
| 9 | `mdi:robot-outline` | 智能助手 | `/knowledge` |
| 10 | `mdi:information-outline` | 关于项目 | `/about` |

---

## 十一、Mock 数据设计

### 11.1 模拟事件数据

系统包含 10 条精心设计的模拟事件，覆盖 8 种风险类型：

| 事件类型 | 风险等级 | 区域 | 场景描述 |
|----------|----------|------|----------|
| 摔倒未响应 | 紧急 | A栋-3层-走廊 | 夜间巡检捕捉到老人在走廊转弯处跌倒 |
| 夜间离床未归 | 高风险 | B栋-5层-房间508 | 凌晨离床超过 15 分钟未返回 |
| 烟火疑似异常 | 高风险 | C栋-1层-茶水间 | 持续性烟雾浓度上升 |
| 长时间滞留 | 中风险 | A栋-1层-电梯口 | 电梯口区域长时间滞留 |
| 久卧未动 | 高风险 | B栋-3层-房间302 | 连续 50 分钟未检测到翻身 |
| 离床未归 | 中风险 | C栋-2层-洗手间通道 | 离开床位后超过预期时间未返回 |
| 冲突 | 低风险 | A栋-2层-活动室 | 两名老人出现争执动作 |
| 摔倒 | 紧急 | B栋-1层-楼梯口 | 楼梯口摔倒，护理员 2 分钟内到场 |
| 无人看护 | 中风险 | C栋-3层-康复区 | 康复区有老人活动但无护理人员 |
| 异常滞留 | 低风险 | A栋-1层-花园入口 | 花园入口处长时间停留 |

### 11.2 模拟住户数据

系统包含 5 位住户的完整行为画像数据：

| 姓名 | 房间 | 年龄 | 风险标签 | 今日偏离度 |
|------|------|------|----------|------------|
| 张建国 | A栋-301 | 78 | 摔倒高危、高血压 | 78% |
| 王秀兰 | B栋-302 | 82 | 压疮风险、骨质疏松 | 65% |
| 陈国华 | B栋-508 | 75 | 夜间离床、行动不便 | 82% |
| 李明辉 | A栋-102 | 80 | 认知障碍、迷路风险 | 45% |
| 刘德华 | B栋-205 | 71 | 步态不稳、膝关节退化 | 92% |

### 11.3 知识库条目

系统内置 5 条护理知识库条目：

| ID | 标题 | 标签 | 适用场景 |
|----|------|------|----------|
| kb-001 | 跌倒应急处理流程 | 摔倒、应急、处置流程 | 所有区域检测到老人跌倒的紧急事件 |
| kb-002 | 夜间离床风险处置规范 | 离床、夜间、巡护 | 夜间 22:00 至次日 6:00 的离床未归事件 |
| kb-003 | 火灾隐患初步处理流程 | 烟火、火灾、安全 | 茶水间、厨房、设备间等高风险区域 |
| kb-004 | 护理员巡房记录标准 | 巡房、记录、标准 | 所有护理区域的日常巡检管理 |
| kb-005 | 老年人突发疾病观察要点 | 突发疾病、观察、急救 | 久卧、异常姿态等疑似疾病相关事件 |

---

## 十二、创新点分析

### 12.1 多模态场景感知

**传统方案**：只能看视频，误报高，不能理解场景。
**安养智巡**：结合视觉、烟雾、声音传感器，对画面有场景理解能力。通过 YOLO 目标检测 + Pose Estimation 姿态估计 + 时序行为分析，实现从"看到"到"看懂"的跨越。

### 12.2 主动风险检测

**传统方案**：护理员每 2 小时巡查一次，中间出事发现不了。
**安养智巡**：系统持续检测风险，一有异常立即通知。将风险发现周期从小时级缩短到分钟级。

### 12.3 知识增强决策

**传统方案**：报警后护理员自己判断要不要处理。
**安养智巡**：系统给出风险等级、判断依据和具体处置建议，并关联知识库条目作为依据，降低护理员的判断压力。

### 12.4 秒级响应能力

**传统方案**：平均 30 分钟以上才发现并通知。
**安养智巡**：从检测到通知平均不超过 4 分钟，关键事件可实现秒级告警。

---

## 十三、适用场景

| 场景 | 说明 |
|------|------|
| 养老院 / 护理院 | 核心应用场景，覆盖摔倒、离床、久卧等常见风险 |
| 康养中心 | 兼顾康复训练安全监控与日常风险检测 |
| 社区日间照料中心 | 白天活动时段的安全监护 |
| 居家养老监护 | 配合家庭摄像头实现居家安全预警 |
| 医院康复病区 | 康复训练区域的无人看护检测与跌倒预警 |

---

## 十四、后端规划

### 14.1 Python 后端技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.11.9 | 运行环境 |
| FastAPI | ≥0.100.0 | Web 框架 |
| Uvicorn | ≥0.23.0 | ASGI 服务器 |
| HTTPX | ≥0.24.0 | 异步 HTTP 客户端 |
| Pydantic | ≥2.0.0 | 数据验证 |
| python-dotenv | ≥1.0.0 | 环境变量管理 |

### 14.2 后端接口预留

当前前端已预留以下后端接口对接能力：

1. **视觉检测接口** (`POST /api/vision/detect`)：预留 YOLO_ENDPOINT 环境变量，支持接入 YOLOv8/YOLO11 目标检测服务
2. **决策分析接口** (`POST /api/decision/evaluate`)：预留 LLM Adapter 接口，支持接入大模型服务
3. **行为分析接口** (`POST /api/behavior/analyze`)：预留真实行为分析引擎接入
4. **RAG 知识库接口**：预留 RAGAdapter 接口，支持接入向量检索服务
5. **AI 聊天接口** (`POST /api/chat`)：已实际对接 OpenAI API

### 14.3 模式切换机制

通过环境变量 `AI_MODE` 控制系统运行模式：

- `AI_MODE=mock`（默认）：使用 Mock 数据和规则引擎
- `AI_MODE=real`：使用真实后端服务，失败时自动降级到 Mock

---

## 十五、部署方案

### 15.1 一键启动

项目提供 `start.bat` 脚本实现一键启动：

1. 检查 Node.js 环境
2. 激活 conda 虚拟环境（如存在）
3. 自动安装前端依赖（首次运行时）
4. 检测并释放 3000 端口
5. 启动 Next.js 开发服务器
6. 等待服务就绪后自动打开浏览器

### 15.2 Vercel 部署

项目已配置 `vercel.json`，支持一键部署到 Vercel 平台：

```json
{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps"
}
```

---

## 十六、总结

**安养智巡**是一个完整的、可部署的前端演示系统，涵盖了从视觉感知、行为理解、智能决策到数据可视化的全链路设计。

**技术亮点**：
- 使用 Next.js App Router + TypeScript + Tailwind CSS 构建，具有清晰的模块划分和良好的代码组织
- 服务层采用适配器模式，支持 Mock / Real 双模式无缝切换
- 统一的 API 响应格式和前端数据获取策略（fallback 兜底）
- AI 聊天助手已实际对接 OpenAI API，其余模块以 Mock 数据模拟但接口完整预留

**产品亮点**：
- 10 个功能页面覆盖完整的安全风险管理流程
- 从风险检测 → AI 决策 → 优先级调度 → 应急引导 → 数据看板的闭环设计
- 注重用户体验，动画流畅、交互友好
- 文案口语化、有感染力，适合作为康养安全 AI 平台的前端原型或比赛演示

**适配能力**：
- 响应式设计，支持桌面端和移动端
- 无障碍友好的色彩对比度
- 网络异常时的数据兜底机制
